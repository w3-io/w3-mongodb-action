{
  "name": "w3-mongodb-action",
  "version": "0.1.0",
  "private": true,
  "description": "MongoDB document store, queries, aggregations, and indexes for W3 workflows",
  "main": "src/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build src/index.js -o dist"
  },
  "type": "module",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@w3-io/action-core": "^0.3.0",
    "mongodb": "^7.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
